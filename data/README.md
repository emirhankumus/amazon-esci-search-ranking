# Data Directory

This directory is reserved for dataset references.

The Amazon ESCI dataset is not stored directly in this repository due to its size.
Instead, the dataset is accessed via Google BigQuery.

Dataset source:
https://github.com/amazon-science/esci-data
